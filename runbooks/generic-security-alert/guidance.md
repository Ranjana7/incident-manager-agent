# Generic security alert guidance

Human checklist:

1. Identify the provider, customer, affected asset, and incident category.
2. Create a more specific runbook if this alert pattern appears repeatedly.
3. Do not perform destructive mitigation until a human validates scope and impact.

Only the structured `runbook.yml` file is executable by the agent.
